name: Quality Guardian TT2

on:
  push:
    branches: [ "main", "refactor/*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  backend-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    # 1. DISK CLEANUP (To gain ~20GB-30GB of space)
    - name: Free Disk Space
      uses: jlumbroso/free-disk-space@v1.3.1
      with:
        tool-cache: true
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: true
        swap-storage: true

    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
        cache: 'pip'

    # 2. FIX FOR OPENCV: Install system libraries for graphics
    - name: Install System Dependencies (OpenCV fix)
      run: |
        sudo apt-get update
        sudo apt-get install -y libgl1-mesa-glx libglib2.0-0

    - name: Install Backend Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest
        # PRO TIP: Install CPU-only Torch to save space/time
        pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
        if [ -f backend/requirements.txt ]; then pip install -r backend/requirements.txt; fi

    - name: Syntax Analysis (Linting)
      run: |
        flake8 backend/ --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 backend/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    # 3. TEST EXECUTION
    - name: Run Tests with Pytest
      run: |
        export PYTHONPATH=$PYTHONPATH:$(pwd)/backend
        pytest backend/tests/test_websockets.py